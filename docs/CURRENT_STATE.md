# CURRENT STATE

> Що вже зроблено, над чим працюємо, що далі. Оновлюється найчастіше з усіх документів.
> Оновлювати в кінці кожної робочої сесії (`/end-session`).

**Оновлено:** 2026-09-22 (кінець сесії)

## Стадія

Phase 1 — Вертикальний зріз. **Кроки 1–2 із 4 виконано: розбір EPUB і API
книги.** Сервер приймає файл, розбирає його і віддає текст частинами;
далі — фронтенд, який це покаже.

## Зроблено

- **Phase 0 — Product Definition:** vision, вимоги R1–R8, рішення D1–D16,
  стек (D15), агенти `prompt-engineer` і `reader-ux`.
- **Phase 1, крок 1 — парсер EPUB (2026-09-10):** пакет `api/` — Node 24 +
  TS без збірки (D20), `src/domain/book.ts`, `src/text/sentences.ts`,
  `src/parsers/{markup,epub}.ts`, `src/cli/parse.ts`. Перевірено на двох
  книгах Gutenberg, але перевірка невідтворювана — див. обмеження.
- **Процес (2026-09-10 і 09-13):** issues (D22), гілки й PR (D21, D24),
  мова (D23), модель на кожен агент/skill (D26), дизайн окремо (D27), два
  рев'ю на PR (D28), один PR — одна тема (D29). Репозиторій публічний (D8)
  з захистом `main`, secret scanning, Dependabot. Локально — deny-правила й
  sandbox поза репозиторієм; `git push` і `gh` ідуть поза sandbox зі свідомим
  підтвердженням автора.
- **2026-09-14, друга сесія — код:**
  - **#49/#50 fixture + тест парсера.** `books/public-domain/pg215-the-call-of-the-wild.epub`
    (Лондон, Gutenberg #215, 228 KB) і `api/src/parsers/epub.test.ts` —
    точні числа (9/342/1686/32141), назви розділів, id (D16), помилка на
    зіпсованому файлі. `.gitignore`: `books/` → `books/*`, інакше виняток не діяв.
  - **#45/#51 CI.** `.github/workflows/ci.yml`: `npm ci`, typecheck, тести на
    кожен PR і push у `main`; `check` — обов'язковий status check, `strict`.
  - **#14/#52 Fastify skeleton.** `api/src/{config,app,server}.ts`,
    `routes/health.ts`, `.env.example`; `npm run dev` / `npm start`. `app.ts`
    збирає застосунок і не слухає порт — тести йдуть через `inject()`;
    `server.ts` — єдине місце з `listen`. Config за D31. Залежності:
    `fastify` 5.12, `@fastify/cors` 11.3. Тестів у проєкті — 23 (було 14). Glob у `npm test` узято
    в лапки — раніше `sh` не бачив тестів прямо в `src/`.
- **2026-09-15:**
  - **#53/#56 рев'ювер на GitHub.** `track_progress: true` (action сам створює
    коментар, модель заповнює його через `mcp__github_comment__update_claude_comment`),
    `--model claude-sonnet-5`, `--max-turns 15`, без `synchronize` — один прогін
    на PR. Workflow виявився вимкненим вручну; після ввімкнення на #60 —
    **працює**: підсумок + 3 inline-коментарі, 6 ходів, 4 хв, $0.70. Ціна
    не впала, бо плагін запускає власних 8 агентів (4 на Opus) → #61.
  - **#57 hooks.** `.claude/settings.json` (у git): `PostToolUse` на `Edit|Write`
    для `api/**/*.ts` і `Stop` при незакомічених `.ts` — typecheck + тести
    (~1.5 с на прогін, таймаут 120 с); при провалі вивід іде моделі (exit 2), Stop дає один шанс виправити
    (`stop_hook_active`). Без `jq` або `api/` — видиме «skipped», exit 1.
    П'ять кіл `/review-pr`; перше знайшло справжній баг (`2>&1 >&2` слав
    помилку в stdout). Потребує `jq` (є в README).
  - **Розвідка парсера на «Дракулі»** (Gutenberg #345, локально, `books/`):
    139 мс, 31 розділ (27 справжніх + 4 службові), 8 871 речення. Листи, телеграми, газетна вирізка —
    абзацами, коректно. Знайдено: → #58 (назва розділу з першого `<h2>` —
    «D R A C U L A»; передмова без заголовка — «Chapter 3»), → #59 (шапка
    Gutenberg як `c0`; ліцензія всередині останнього розділу, не окремою
    сторінкою), → коментар до #22 (рядки віршів клеяться в одне речення;
    непарні лапки в багатоабзацних листах; дати `3 May.` як речення).
  - **Процес.** Сесія починається з плану за цим документом; кожен крок —
    після згоди авторки; розвідка й експерименти — теж крок.

- **2026-09-22 — API книги (#15, #16):**
  - **#15/#62 `POST /books`.** Upload через `multipart/form-data`
    (`@fastify/multipart` 10.1.1), парсинг на сервері, `BookStore` у
    пам'яті; id — хеш вмісту, ліміт 20 MB, LRU на 10 книг (D32). Відповіді
    про помилку — один формат `{ error: { reason, message } }`: 400
    `no-file`/`not-multipart`, 413 `too-large`, 422 з причиною
    `BookParseError`. Обробник помилок — усередині плагіна, тож `/health`
    його не бачить.
    Ліміт розміру довелося робити двічі: `toBuffer()` кидає помилку лише
    коли частина закінчилася, тому весь файл встигав прийти з мережі.
    Тепер — гонка `toBuffer()` проти події `limit` від busboy плюс
    `connection: close` і `request.raw.destroy()`. Виміряно: 200 MB
    відкинуто після 21 MB за 457 мс.
  - **#16/#63 `GET /books/:id` і `/books/:id/chapters/:index`.** Список
    розділів із лічильниками й без тексту (1.1 KB проти ~400 KB книги),
    розділ — як доменний `Chapter` з id речень (D16). 404 для невідомої
    книги з підказкою «завантажте ще раз», 404 для неіснуючого розділу,
    400 `bad-index` для індексу не з цифр (`Number("1e2")` пройшов би).
    `BookStore.get` освіжає позицію — витіснення стало справжнім LRU.
  - Тестів у проєкті: 23 → 40. Hooks відпрацювали в живій сесії — typecheck
    і тести після кожної правки `.ts`, як і задумано.

## Наступний крок

1. **#61 — переробити GitHub-рев'ювера.** Свій промпт і 2–3 агенти замість
   плагіна `code-review`: він запускає власних 8 агентів, через що job
   виходить за `--max-turns 15` (на #62 — 22 ходи, на #63 — 17) і коштує
   ~$0.70 навіть на Sonnet. Червоний статус не блокує злиття, але він
   червоний на кожному PR, і це неправда про стан коду.
2. **#18** React + Vite скелет і клієнт API — початок фронтенду; перед ним
   **#40** (дизайн екранів) і **#19** (типографіка, O7).
3. **#39** — схеми відповідей і `/docs`: свідомо відкладено з #16, щоб
   додати схеми всім роутам разом.
4. **#58, #59** — після вертикального зрізу, не блокують.

Вертикальний зріз:

> ~~книга EPUB → розбір на сервері → HTTP-скелет → API книги~~ →
> **показ тексту в reader** → **виділення слова** →
> **переклад речення з підсвіченою відповідністю**

## Відомі обмеження та ризики

- **GitHub-рев'ювер дорогий і завжди червоний:** $0.70 за docs-PR #60 при
  Sonnet і 6 ходах — вартість у субагентах плагіна `code-review`, які
  `--model` не змінює. На PR із кодом той самий плагін виходить за
  `--max-turns 15` (#62 — 22 ходи, #63 — 17): рев'ю публікується повністю,
  але job падає з `exit 1`. Обов'язковий статус лише `CI / check`, тож
  злиттю це не заважає. Підняти ліміт — пластир; рішення — #61.
- **Локальний рев'ювер недетермінований:** на одній і тій самій гілці
  кожне коло дає нову дрібну 🟡 (у #57 — п'ять кіл; те, що в одному
  проході ℹ️, у наступному 🟡). Критерій зупинки — «без 🔴, 🟡 виправлені
  або свідомо відхилені», не «нуль зауважень». Варіант на майбутнє —
  жорсткіший чекліст у `code-reviewer` для shell-команд (потоки
  stdout/stderr, відсутні утиліти, тихі `exit 0`).
- **Книги живуть лише в пам'яті (D19, D32):** рестарт сервера або десять
  нових завантажень — і читач отримує 404 посеред книги. Фронтенд має це
  обробляти, а не вважати id вічним.
- **`chapters[index]` покладається на те, що позиція в масиві дорівнює
  `chapter.index`.** Для EPUB-парсера це так; другий парсер може це
  зламати тихо.
- **`$TMPDIR` усередині sandbox і поза ним — різні каталоги.** Через це в
  PR #62 опис поїхав із чужої гілки (файл писався в один каталог, читався
  з іншого). Файли для `gh` — за абсолютним шляхом у scratchpad, і опис
  перевіряти після `gh pr create`, а не до.
- **Тест фіксує службові сторінки Gutenberg як розділи** (`c0`, `c8`).
  Коли #59 їх відфільтрує — тест зламається навмисно, числа оновити.
- **Абзаци в голих `<div>`** (у «Дракулі» `div.poem` розібрано — але як
  одне речення, див. #22), список скорочень у сегментації (D18) — без змін.
- **Свідомо відхилено в #63:** лічильники речень рахуються на кожен
  `GET /books/:id`, а не зберігаються в `Chapter` — мікросекунди проти
  розширення доменної моделі (D6) без виміряної проблеми.
- **Побічне, окремими гілками, не терміново:** `docs/README.md` написаний
  англійською, тоді як решта `docs/` — українською (D23) — свідомо чи ні,
  не вирішено; skill `/end-session`, крок 4 «запитати про коміт», написаний
  до D21 і не згадує гілку та PR; `Bash(gh pr comment:*)` у
  `--allowedTools` рев'ювера, ймовірно, зайвий після `track_progress`;
  «Аліса» (Gutenberg #11) — наступна книга для розвідки, вірші й курсив.

## Відкриті питання (OPEN)

| # | Питання | Коли треба вирішити |
|---|---|---|
| O2 | Модель Claude для підказок і формат структурованого виходу | На вертикальному зрізі |
| O4 | Джерела CEFR-даних і частотності + ліцензування | Перед R8 |
| O5 | Як рахувати «об'єктивну складність» (D2) | Перед R8 |
| O6 | Правові межі публічного демо з користувацьким upload (D10) | Перед публічним деплоєм (#46) |
| O7 | Типографіка reader: шрифт, міра рядка, ритм, теми | Наступний крок (#19, #40) |
| O8 | Вибір ORM | Коли з'явиться база (D19) |
| O9 | Як передавати й зберігати мову допомоги (D25) | Перед промптом підказки (#22) |

## Закриті питання

O1 (стек, D15), O3 (зберігання книги, D15/D16), O8 частина про парсери (D17),
питання з розділу 29 блупринту.

## Не робимо (щоб не спокушатися)

Бібліотека книг, словник, модель знань користувача, аудіо, квізи, переписування
тексту під рівень, повний переклад книги, складна агентна архітектура.
**Також:** MCP-сервери, plugins, agent teams, нові агенти й нові правила в
`CLAUDE.md` — поки не з'явиться конкретна потреба. CD — не раніше Phase 3 (#46).

## Команди

```
gh issue list --milestone "Phase 1 — Vertical slice" --limit 50   # що робимо зараз
gh issue list --label epic --limit 50                             # мапа проєкту
```
