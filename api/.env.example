# Copy to .env for local development; .env is ignored by git.
# Every variable the server reads is listed here. Values are examples.

PORT=3000
HOST=127.0.0.1

# Origin the frontend is served from (Vite dev server default). Required.
CORS_ORIGIN=http://localhost:5173
